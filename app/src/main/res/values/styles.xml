<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/holo_blue_light</item>
    </style>
    <style name="CardViewStyleMain" parent="CardView">
        <item name="cardElevation">8dp</item>
        <item name="cardCornerRadius">6dp</item>
    </style>

    <style name="SettingsSwitch">
        <!-- Active thumb color & Active track color(30% transparency) for Switch compat -->
        <item name="colorControlActivated">@color/text_red</item>
    </style>
</resources>
